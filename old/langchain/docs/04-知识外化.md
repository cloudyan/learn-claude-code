# 第4章：知识外化 —— v4 Skills 机制

**~350 行代码，+2 个工具，领域知识按需加载。**

---

## 痛点：在 Prompt 里堆金子

你有没有过这种感觉：

```
SYSTEM_PROMPT = """
你是一个全能的 AI 助手。

关于 Web 开发：
- React 钩子规则...
- TypeScript 最佳实践...
- 等等，还有 Node.js...

关于数据库：
- Postgres 索引...
- 等等，还有 MongoDB...

还有 Git 工作流...
还有代码审查清单...
还有...
"""
```

问题：
- 上下文很贵
- 大部分知识 99% 时间没用
- 加的知识越多，越容易互相干扰

---

## 核心洞察：工具 vs 技能

| 维度 | 工具 | 技能 |
|------|------|------|
| **是什么** | 你能做什么 | 你知道什么 |
| **例子** | `bash`, `read_file` | "如何写 React 组件" |
| **存储** | 代码里 | `SKILL.md` 文件 |
| **加载** | 总是加载 | 按需加载 |
| **使用** | 模型调用 | 模型阅读 |

---

## SKILL.md 格式

YAML frontmatter + Markdown 正文：

```markdown
---
name: React 组件设计
description: 如何设计可复用的 React 组件
---

# React 组件设计指南

## 单一职责原则

每个组件只做一件事...

## Props 设计

- 保持 props 简单
- 避免过度封装
- ...
```

---

## SkillsLoader 实现

简单实现，不依赖第三方库：

```typescript
interface Skill {
  name: string;
  description: string;
  content: string;
}

class SkillsLoader {
  private cache: Map<string, Skill> = new Map();
  private skillsDir: string;

  constructor(skillsDir: string) {
    this.skillsDir = skillsDir;
  }

  parseSkill(filePath: string): Skill | null {
    if (this.cache.has(filePath)) {
      return this.cache.get(filePath)!;
    }

    if (!existsSync(filePath)) {
      return null;
    }

    const content = readFileSync(filePath, 'utf8');

    // 解析 YAML frontmatter
    const frontmatterMatch = content.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/);

    let name = path.basename(filePath, '.md');
    let description = '';
    let body = content;

    if (frontmatterMatch) {
      const frontmatter = frontmatterMatch[1];
      body = frontmatterMatch[2];

      // 简单解析 YAML
      const nameMatch = frontmatter.match(/name:\s*(.+)/);
      const descMatch = frontmatter.match(/description:\s*(.+)/);

      if (nameMatch) name = nameMatch[1];
      if (descMatch) description = descMatch[1];
    }

    const skill: Skill = { name, description, content: body };
    this.cache.set(filePath, skill);
    return skill;
  }

  listAvailableSkills(): string {
    // ... 列出 skills 目录下的 .md 文件
  }
}
```

---

## 两个新工具

```typescript
{
  type: 'function',
  function: {
    name: 'load_skill',
    description: '加载技能文件（按需加载领域知识）',
    parameters: {
      type: 'object',
      properties: {
        skill_name_or_path: {
          type: 'string',
          description: '技能名称或 SKILL.md 文件路径'
        }
      },
      required: ['skill_name_or_path']
    }
  }
},
{
  type: 'function',
  function: {
    name: 'list_skills',
    description: '列出所有可用的技能',
    parameters: { type: 'object', properties: {} }
  }
}
```

---

## 工作模式

```
你: "帮我写一个 React 组件。"

v4: "我先看看有什么技能。"
    [load_skill: "React 组件设计"]
    "懂了，我按照这个指南来写。"
    [写组件...]
```

---

## 核心洞察（记住这个）

> **不要在 prompt 里堆金子，按需加载。**

技能太多怎么办？加个"技能索引"技能，让模型自己决定加载什么。

---

## 试试看

```bash
npm run v4 -- "列出可用的技能，然后加载一个"
```

---

**下一章，我们回到原点，看看 v0 —— 100 行代码（甚至更少）的终极简化。**
